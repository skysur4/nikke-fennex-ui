import { styled } from '@mui/material/styles';

const RedCode = styled('span')(({ theme }) => ({
  borderRadius: 5,
  backgroundColor: '#eeeeee',
  color: '#2a4b7c',
  padding: '0 5px',
}));

export default RedCode;
